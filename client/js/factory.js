importScripts('settings.js');

var cache = {};

var sampleAverageColor = function(data) {

  // read rgb values from random location in the array

  // var x = 0, y = 0;
  // var pixel = image.getImageData(x, y, 1, 1).data;
  // console.log(pixel);

};

var reduceColor = function(r, g, b) {
  var hex;
  return hex;
};

// COLOR_DEPTH = 16;
// SAMPLE_RATE = 8;